# Graphify — cache local (regenerável, pode crescer)
graphify-out/cache/

# Firebase — chaves do projeto (legado; o HTML não referencia mais; mantido
# no gitignore para que arquivos locais antigos não vazem se ainda existirem).
web/firebase-config.local.js

.env
.env.local
.env.*.local
node_modules/
.vercel

# Editor / SO
.DS_Store
Thumbs.db
*.swp
*~

# Ferramentas
.eslintcache

# Vite (Onda 2)
dist/
.vite/
